# Demo Scan Repo

This repository is a small demo repo containing a vulnerable Python script and a JavaScript file to demonstrate the scanner.

Use owner `local` and repo `demo-scan-repo` is not a GitHub repo; instead clone locally via file path if needed. The API expects GitHub owner/repo, so you can scan `octocat/Hello-World` as a quick demo.
